.dashboard-layout {
  display: flex;
  height: 100vh;
}

/* FIXED SIDEBAR */
.sidebar {
  width: 240px;
  position: fixed;
  top: 0;
  left: 0;
  bottom: 0;
  background: #ffffff;
  border-right: 1px solid #ddd;
}

/* RIGHT AREA = Navbar + content */
.right-section {
  margin-left: 240px; /* IMPORTANT → push right section */
  width: calc(100% - 240px);
  display: flex;
  flex-direction: column;
  height: 100vh;
}

/* FIXED NAVBAR */
.navbar {
  height: 60px;
  background: white;
  border-bottom: 1px solid #ddd;
  position: fixed;
  top: 0;
  left: 240px; /* IMPORTANT → align next to sidebar */
  right: 0;
  display: flex;
  align-items: center;
  padding-left: 20px;
  z-index: 10;
}

/* PAGE CONTENT BELOW NAVBAR */
.page-content {
  margin-top: 60px; /* IMPORTANT → avoid navbar overlapping */
  padding: 20px;
  overflow-y: auto;
  height: calc(100vh - 60px);
  background: #fafafa;
}
